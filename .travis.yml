language: cpp
compiler:
- clang
- gcc
matrix:
  allow_failures:
  - compiler: clang
notifications:
  email:
  - francois.keith@gmail.com
branches:
  only:
  - master
  - groovy
script: ./.travis/build
before_install: ./.travis/before_install
after_success: ./.travis/after_success
